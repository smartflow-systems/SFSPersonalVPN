{
  "name": "sfs-personal-vpn",
  "version": "1.0.0",
  "description": "SFS PersonalVPN - Secure VPN with real-time security alerts, extended analytics, and user preferences",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "serve": "python3 -m http.server 8080 --directory public",
    "test": "echo 'Tests to be implemented' && exit 0",
    "sync": "bash scripts/auto-sync.sh",
    "pull": "git pull --no-edit",
    "push": "git push",
    "auto-commit": "git add . && git commit -m 'chore: auto-commit [Auto-Sync]' || true",
    "sync-all": "npm run auto-commit && npm run sync"
  },
  "keywords": [
    "vpn",
    "security",
    "privacy",
    "analytics",
    "smartflow"
  ],
  "author": "SmartFlow Systems",
  "license": "MIT",
  "dependencies": {
    "tailwindcss": "^3.4.0",
    "autoprefixer": "^10.4.16",
    "postcss": "^8.4.32"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/smartflowsystems/sfs-personal-vpn"
  }
}
